# Build
dist
dist-dev
lib
lib-types
build
tmp

# Development
node_modules
.pnpm-store
.env
*.local

# Cache
.cache
tsconfig.tsbuildinfo

# Local libSQL dev database (see scripts/seed.ts) — never lives under `app`
.turso-state

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Editor
.history
.vscode/*
!.vscode/launch.json
!.vscode/*.code-snippets
# Shared, not personal — maps config/*.json to their JSON Schemas so every
# contributor's editor gets live validation, see config/README.md.
!.vscode/settings.json
.idea
.DS_Store
