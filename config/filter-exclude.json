[
  {
    "sources": ["pacman-aur"],
    "name": "litehtml0.9",
    "reason": "Real library — \"Fast and lightweight HTML/CSS rendering engine\", embedded in other apps, not launchable on its own. No safe general pattern (versioned-library names like this collide with real apps that also carry a bare version number, e.g. 0ad/7zip/openttd)."
  },
  {
    "sources": ["pacman-aur"],
    "name": "cs-signal",
    "reason": "Real library — \"Library for thread aware signal/slot delivery\" (CopperSpice's signal/slot mechanism). No safe general pattern: re-checked the whole real cs-* family on a later pass looking for one — cs-bin (\"codespelunker\", a real CLI code-search tool), cs-prolog (a real standalone Prolog interpreter), and cs-script (a real standalone C# script runner) are all real, unrelated tools that just happen to share the CopperSpice prefix coincidentally, confirming a \"cs-\" pattern would be unsafe."
  },
  {
    "sources": ["pacman-aur"],
    "name": "claude-cowork-rs",
    "reason": "Real support package — \"Linux daemon for Claude Desktop Cowork (Local Agent Mode)\", a background helper process for a different app (Claude Desktop), not launchable on its own. No name-pattern would catch this (the word \"daemon\" is only in the description, not the name)."
  },
  {
    "sources": ["rpm-fedora"],
    "name": "biosig4c++",
    "reason": "Real library — \"A software library for processing of biomedical signals\", hasDesktopFile:false. No safe general pattern: a bare \"c++\" substring check has real counter-examples (Gentoo's c++robots, a real King-of-the-Hill programming game)."
  },
  {
    "sources": ["ebuild-gentoo"],
    "name": "adblock",
    "reason": "Same real library as filter/rules.ts's python-adblock pattern (Brave's adblock-parsing engine), but Gentoo's dev-python naming convention doesn't carry the python-prefix that pattern matches on — bare \"adblock\" isn't safe to generalize (Gentoo's dev-python category itself isn't safe to exclude wholesale either — verified live it mixes in real CLI tools: black, bpython, ipython, poetry)."
  },
  {
    "sources": ["github-releases"],
    "name": "AppImageKit",
    "reason": "Real packaging tooling — \"Package desktop applications as AppImages\" — not itself a launchable app, despite carrying the linux-app topic and a real tagged Release. The exact precision risk flagged during this connector's own scoping (GitHub Releases connector card)."
  },
  {
    "sources": [
      "deb-debian",
      "deb-ubuntu",
      "eopkg-solus",
      "nix-nixpkgs",
      "pacman-arch",
      "rpm-fedora",
      "rpm-opensuse",
      "xbps-void"
    ],
    "name": "0ad-data",
    "reason": "0ad's own companion data-files package — every description literally says \"(data files)\"/\"The Data Files for 0 AD\" — not launchable on its own, the real game is the separate \"0ad\" package already in the catalog. User-reported. No safe general pattern: a bare `-data$` suffix matches 1,291 names across the whole dataset, mostly the same companion-data shape but not individually verified, so not blanket-excluded here."
  },
  {
    "sources": ["deb-debian", "deb-ubuntu"],
    "name": "0ad-data-common",
    "reason": "Same 0ad companion data as \"0ad-data\" above — Debian/Ubuntu split it into a \"-common\" sub-part (\"Real-time strategy game of ancient warfare (common data files)\"). User-reported."
  },
  {
    "sources": ["pacman-aur"],
    "name": "0ad-data-git",
    "reason": "AUR's git-build variant of \"0ad-data\" itself, not of \"0ad\" — match/group.ts's tier2Key strips the `-git` suffix, so this unions with the other \"0ad-data\" packages above rather than the real game. User-reported."
  },
  {
    "sources": [
      "deb-debian",
      "deb-ubuntu",
      "ebuild-gentoo",
      "eopkg-solus",
      "pacman-aur",
      "rpm-fedora",
      "snap-snapcraft"
    ],
    "name": "snapd",
    "reason": "The snapd daemon/tooling itself — \"Daemon and tooling that enable snap packages\"/\"Service and tools for management of snap packages\" — background plumbing, not a launchable end-user app. User-reported. rpm-fedora's entry has hasDesktopFile:true despite this, so the GUI/desktop-file signal alone doesn't rescue it."
  }
]
