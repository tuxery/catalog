{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Enrich: software suites",
  "description": "Software suites (a bundled \"main\" app plus separately installable \"component\" apps — e.g. LibreOffice/Writer/Calc/...) that aren't a single-app matching decision, so don't fit filter/match's override shapes. Deliberately not auto-detected from names — curated by hand and narrow rather than pattern-matched.",
  "type": "array",
  "items": {
    "type": "object",
    "required": ["suiteId", "suiteName", "mainAppId", "components", "reason"],
    "additionalProperties": false,
    "properties": {
      "suiteId": {
        "type": "string",
        "description": "A stable, unique identifier for this suite (not shown to users)."
      },
      "suiteName": {
        "type": "string",
        "description": "The suite's display name, e.g. \"LibreOffice\"."
      },
      "mainAppId": {
        "type": "string",
        "description": "The bundled/full-suite app's own catalog id (source:appId), e.g. \"flatpak-flathub:org.libreoffice.LibreOffice\"."
      },
      "components": {
        "type": "array",
        "minItems": 1,
        "items": {
          "type": "object",
          "required": ["appId", "name"],
          "additionalProperties": false,
          "properties": {
            "appId": {
              "type": "string",
              "description": "The component's own catalog id (source:appId)."
            },
            "name": {
              "type": "string",
              "description": "The component's display name, e.g. \"LibreOffice Writer\"."
            }
          }
        },
        "description": "Every independently-installable component of this suite."
      },
      "reason": {
        "type": "string",
        "description": "How this was verified — required so the grouping is auditable later, not just an unexplained line."
      }
    }
  }
}
