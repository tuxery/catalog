# Sources

Every upstream Linux app source Tuxery could pull from, in priority order.
Goal (probably unreachable, and that's fine): list Linux apps exhaustively.
Status here should stay in sync with the "connector" cards on the
[Tuxery GitHub Project](https://github.com/orgs/tuxery/projects/1) — this
table is the map, the Project is the tracked work.

## Support matrix

| #   | Source                                                                                          | Format                                               | Mechanism                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Exhaustive?                                                                               | Status                                                                                                                                                                         |
| --- | ----------------------------------------------------------------------------------------------- | ---------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | **Flathub**                                                                                     | Flatpak                                              | `dl.flathub.org/repo/appstream/x86_64/appstream.xml.gz` — the appstream repodata Flatpak clients themselves consume. Single gzipped XML file, no auth, no pagination.                                                                                                                                                                                                                                                                                                                                | ✅ Yes — the canonical catalog                                                            | **Implemented** — `packages/sources/flathub/fetch.ts`, cache has 3,345 apps                                                                                                    |
| 1b  | **Other Flatpak remotes** (GNOME nightly, KDE `kdeapps`, Fedora's own flatpak remote, ...)      | Flatpak                                              | Same appstream.xml.gz mechanism per remote, different host. Not investigated yet.                                                                                                                                                                                                                                                                                                                                                                                                                    | ⚠️ Niche — mostly nightly/testing builds, not curated apps                                | Not started — low priority, Flathub already covers the vast majority of published apps                                                                                         |
| 2   | **Snapcraft**                                                                                   | Snap                                                 | `api.snapcraft.io/v2/snaps/find?category=<name>` (`Snap-Device-Series: 16` header) — 100 results/category, no pagination. Categories from `api.snapcraft.io/api/v1/snaps/sections` (v1 API — v2 has no equivalent listing endpoint).                                                                                                                                                                                                                                                                 | ⚠️ Approximate — no single dump, coverage capped by the 20-category sweep                 | **Implemented** — `packages/sources/snapcraft/fetch.ts`, cache has 1,542 snaps                                                                                                 |
| 3   | **AppImage**                                                                                    | AppImage                                             | [`appimage.github.io/feed.json`](https://appimage.github.io/feed.json) (AppImageHub, community-curated), filtered to entries with a GitHub repo link (~3 in 4 of ~1,400). No real `version` yet — that needs a per-repo GitHub Releases lookup, not implemented ("AppImage version resolution" card).                                                                                                                                                                                                | ⚠️ Depends on community curation — not every AppImage publisher is listed                 | **Implemented** (name/description/icon/repo) — `packages/sources/appimage/fetch.ts`, cache has 1,104 apps                                                                      |
| 4   | **GitHub Releases** (generic)                                                                   | Any (`.deb`, `.rpm`, `.AppImage`, raw binaries, ...) | No catalog exists — would need GitHub topic/code search heuristics (e.g. `topic:linux-app`) or a curated seed list (awesome-lists style). Shares plumbing with AppImage's per-repo Release lookup.                                                                                                                                                                                                                                                                                                   | ❌ Not exhaustive by nature — scope narrowly (topics) rather than attempt to crawl GitHub | **Deferred to roadmap** — not convincing enough yet to build; not a package                                                                                                    |
| 5a  | **AUR** (Arch, community)                                                                       | Native                                               | `aur.archlinux.org/packages-meta-ext-v1.json.gz` — full metadata dump, regenerated every ~5 min. Single file, no auth.                                                                                                                                                                                                                                                                                                                                                                               | ✅ Yes — full dump, easiest native source                                                 | **Implemented** — `packages/sources/aur/fetch.ts`, cache has 117,520 packages (bigger than expected)                                                                           |
| 5b  | **Arch official** (core + extra + multilib)                                                     | Native                                               | `geo.mirror.pkgbuild.com/{core,extra,multilib}/os/x86_64/{core,extra,multilib}.db` — gzipped tar archives, one `desc` file per package (`%FIELD%\nvalue\n\n` format, not deb822/XML/JSON), extracted via the `tar` npm package. **Distinct from AUR** — pre-built, Arch-team-maintained packages, not community build recipes.                                                                                                                                                                       | ✅ Yes, per repo/arch combination                                                         | **Implemented** — `packages/sources/arch/fetch.ts`, cache has 15,474 packages (core+extra+multilib merged)                                                                     |
| 5c  | **Debian**                                                                                      | Native                                               | `deb.debian.org/debian/dists/stable/{main,contrib,non-free,non-free-firmware}/binary-amd64/Packages.gz` — `.gz`, not the archive's default `.xz` (Node's built-in zlib can gunzip without a new dependency; Debian still publishes both). deb822 stanza format. stable/amd64 only — other suites/archs are the same mechanism, not implemented.                                                                                                                                                      | ✅ Yes, per suite/component/arch combination                                              | **Implemented** (stable, all 4 components, amd64 only) — `packages/sources/debian/fetch.ts`, cache has 69,843 packages                                                         |
| 5d  | **Ubuntu**                                                                                      | Native                                               | Same deb822 mechanism as Debian (it's a derivative), but **needs all four components**, not `main` alone — verified against the real archive: `main` alone yielded 6,487 packages, adding `universe` brought it to 73,219. Ubuntu's component split is by _support tier_ (Canonical vs. community) and legal status, unlike Debian's purely license-based split, so most desktop apps live in universe. resolute (26.04)/amd64 only — other suites/archs not fetched.                                | ✅ Yes, per suite/components/arch combination                                             | **Implemented** (resolute, all 4 components, amd64 only) — `packages/sources/ubuntu/fetch.ts`, cache has 75,328 packages                                                       |
| 5e  | **Fedora**                                                                                      | Native                                               | Two-step per repo: `repodata/repomd.xml` first (to find the current content-hashed `primary.xml.zst` path — RPM repos don't use a fixed filename like Debian's `Packages.gz`), then that file, Zstandard-compressed (Node 24's built-in zlib decodes it, no new dependency). Fetches both release 44's Everything repo (the frozen release-day snapshot) and its updates overlay, merged by name (updates wins ties — matches real dnf/yum behavior); x86_64 only, other releases/archs not fetched. | ✅ Yes, per release/repo/arch combination                                                 | **Implemented** — `packages/sources/fedora/fetch.ts`, cache has 68,990 packages (merged & deduped by name — Everything alone has 76,354 raw rows but only 67,430 unique names) |
| 5f  | **Other native** (openSUSE, Alpine, NixOS/nixpkgs, Void, Gentoo, Solus, Clear Linux, Slackware) | Native                                               | Each has its own repodata format; openSUSE/Alpine/Void are the same "repodata file(s), per repo/arch" shape as 5a–5e; Nix is a different paradigm entirely (functional package management) with its own search API (search.nixos.org); Gentoo/Solus/Slackware/Clear Linux are niche enough to be low priority.                                                                                                                                                                                       | ✅ Yes, per distro                                                                        | Roadmap — openSUSE/Alpine/Nix worth prioritizing over Gentoo/Solus/Slackware/Clear Linux when picked up                                                                        |

## Notes

- **Two-stage model per source**: `fetch.ts` (network → `packages/sources/cache/<source>.ndjson`, source-specific row type) and `normalize.ts` (cache → the shared `SourcedPackage` type). Only `fetch.ts` needs implementing per row above — `normalize.ts`, the cache read path, and tests already exist per source (see the root [`AGENTS.md`](../AGENTS.md)'s "Source cache" section).
- **Fetch metadata**: every implemented `fetch.ts` writes a `cache/<source>.meta.json` sidecar — `fetchedAt`, the upstream `url`, `entryCount`, plus source-specific details (Flathub's `arch`, Snapcraft's `deviceSeries`/`categoriesSwept`). See `_shared/metadata.ts`.
- **Matching cost**: `@tuxery/curator`'s `match/group.ts` (formerly the standalone `@tuxery/matcher` package — merged into `curator` alongside the new `filter/` stage) originally bucketed by an 8-char normalized-name prefix before pairwise scoring within each bucket — a mitigation over a naive full pairwise scan, but one whose cost still climbed super-linearly as more sources landed: ~6s at ~123k packages, ~26s at ~192k, ~51s at ~268k, ~111s at ~357k raw, ~35s after `curator/filter` cut the input to ~303k (see the "Matcher bucket sizes growing again" card, now closed). It's since been replaced with a union-find over exact-match tiers — manual overrides → exact `appId` → exact normalized name, each an O(1)-per-package map lookup, no pairwise comparison at all. A fourth, fuzzy/scored tier (`match/score.ts`'s `scoreMatch`, name-distance + appId + icon-filename weights) was considered but turned out to be mathematically unreachable once the two exact tiers run first — with the current weights (name 0.5, appId 0.35, icon 0.15) and a 0.75 threshold, no pair lacking an exact appId or exact name can score above 0.65 — so it was dropped rather than shipped as inert code; `scoreMatch` stays exported for when weights get revisited. Net effect: grouping the filtered ~303k packages now takes well under a second.
- **Scheduled refresh**: `fetch.ts` implementations are meant to run on a cron schedule, not on every push — see the "Wire scheduled source refresh" card on the Tuxery GitHub Project.
- **Known incompleteness, tracked as roadmap cards**: Snapcraft's 20-category sweep is capped at 100 results/category, so its cache is very likely a fraction of the real store ("Fix Snapcraft exhaustiveness"). AppImage drops ~22% of AppImageHub's entries (no resolvable GitHub link) and has no real `version` yet ("Fix AppImage exhaustiveness and veracity" — also worth checking AppImageHub isn't the only community catalog worth pulling from).
- **Catalog filtering**: `@tuxery/curator`'s `filter/` drops packages that look like libraries/dev-headers/docs/fonts/language-ecosystem-modules rather than apps/games — every pattern verified against real cache data before landing, including a blanket `^lib` noise prefix with real exceptions (LibreOffice, LibreCAD, Libreddit, ...) rescued by exact name via `overrides/keep.ndjson` rather than by pattern (a `libre*`-prefix allowlist was considered and rejected: 1,208 unique `libre*` names exist, only 25 are real). Effective on Debian (~49.9%), Ubuntu (~48.5%), and Fedora (~54.7%); much less so on AUR (~7.4%) and Arch official (~17.6%) — those ecosystems don't split `-dev` packages out or soname-version names the way Debian does, so there's less naming signal to key off. A reverse-dependency-graph signal and AUR's self-declared Keywords field were both investigated as a further AUR/Arch-specific improvement and found not viable (see the "Filter is far less effective on AUR/Arch" card for the full research writeup) — the ticket stays open for a genuinely new idea. `overrides/keep.ndjson` and `exclude.ndjson` (in `packages/curator/overrides/`) are the manual escape hatch on either side; see that directory's `README.md` for the "would a user launch this on its own" litmus test used to decide `keep.ndjson` entries.
