# Sources

Every upstream Linux app source Tuxery could pull from, in priority order.
Goal (probably unreachable, and that's fine): list Linux apps exhaustively.
Status here should stay in sync with the "connector" cards on the
[Tuxery GitHub Project](https://github.com/orgs/tuxery/projects/1) — this
table is the map, the Project is the tracked work.

## Support matrix

| #   | Source                                                                                     | Format                                               | Mechanism                                                                                                                                                                                                                            | Exhaustive?                                                                               | Status                                                                                       |
| --- | ------------------------------------------------------------------------------------------ | ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| 1   | **Flathub**                                                                                | Flatpak                                              | `dl.flathub.org/repo/appstream/x86_64/appstream.xml.gz` — the appstream repodata Flatpak clients themselves consume. Single gzipped XML file, no auth, no pagination.                                                                | ✅ Yes — the canonical catalog                                                            | **Implemented** — `packages/sources/flathub/fetch.ts`, cache has 3,342 apps                  |
| 1b  | **Other Flatpak remotes** (GNOME nightly, KDE `kdeapps`, Fedora's own flatpak remote, ...) | Flatpak                                              | Same appstream.xml.gz mechanism per remote, different host. Not investigated yet.                                                                                                                                                    | ⚠️ Niche — mostly nightly/testing builds, not curated apps                                | Not started — low priority, Flathub already covers the vast majority of published apps       |
| 2   | **Snapcraft**                                                                              | Snap                                                 | `api.snapcraft.io/v2/snaps/find?category=<name>` (`Snap-Device-Series: 16` header) — 100 results/category, no pagination. Categories from `api.snapcraft.io/api/v1/snaps/sections` (v1 API — v2 has no equivalent listing endpoint). | ⚠️ Approximate — no single dump, coverage capped by the 20-category sweep                 | **Implemented** — `packages/sources/snapcraft/fetch.ts`, cache has 1,542 snaps               |
| 3   | **AppImage**                                                                               | AppImage                                             | [`appimage.github.io/feed.json`](https://appimage.github.io/feed.json) (AppImageHub, community-curated) as a seed list of GitHub repos, then the GitHub Releases API per repo to find the actual `.AppImage` asset + version.        | ⚠️ Depends on community curation — not every AppImage publisher is listed                 | Scaffolded (`packages/sources/appimage/`), reads an empty cache — `fetch.ts` not implemented |
| 4   | **GitHub Releases** (generic)                                                              | Any (`.deb`, `.rpm`, `.AppImage`, raw binaries, ...) | No catalog exists — would need GitHub topic/code search heuristics (e.g. `topic:linux-app`) or a curated seed list (awesome-lists style). Shares plumbing with AppImage's per-repo Release lookup.                                   | ❌ Not exhaustive by nature — scope narrowly (topics) rather than attempt to crawl GitHub | Not started, not yet a package                                                               |
| 5a  | **AUR** (Arch)                                                                             | Native                                               | `aur.archlinux.org/packages-meta-ext-v1.json.gz` — full metadata dump, ~90k packages, regenerated every ~5 min. Single file, no auth.                                                                                                | ✅ Yes — full dump, easiest native source                                                 | Not started, not yet a package                                                               |
| 5b  | **Debian / Ubuntu**                                                                        | Native                                               | `deb.debian.org/debian/dists/<suite>/main/binary-<arch>/Packages.xz` (and Ubuntu's equivalent archive) — one file per suite/component/arch. deb822 stanza format, not JSON.                                                          | ✅ Yes, per suite                                                                         | Not started, not yet a package                                                               |
| 5c  | **Fedora**                                                                                 | Native                                               | `repodata/primary.xml.gz` per Fedora repo/mirror.                                                                                                                                                                                    | ✅ Yes, per repo                                                                          | Not started, not yet a package                                                               |
| 5d  | **Other native** (openSUSE, Alpine, Void, Nix, ...)                                        | Native                                               | Each has its own repodata format; broadly similar shape to Debian/Fedora.                                                                                                                                                            | ✅ Yes, per distro                                                                        | Not planned — extend once 5a–5c land                                                         |

## Notes

- **Two-stage model per source**: `fetch.ts` (network → `packages/sources/cache/<source>.ndjson`, source-specific row type) and `normalize.ts` (cache → the shared `SourcedPackage` type). Only `fetch.ts` needs implementing per row above — `normalize.ts`, the cache read path, and tests already exist per source (see the root [`AGENTS.md`](../AGENTS.md)'s "Source cache" section).
- **Fetch metadata**: every implemented `fetch.ts` writes a `cache/<source>.meta.json` sidecar — `fetchedAt`, the upstream `url`, `entryCount`, plus source-specific details (Flathub's `arch`, Snapcraft's `deviceSeries`/`categoriesSwept`). See `_shared/metadata.ts`.
- **Matching cost**: once native sources (tens of thousands of packages) land alongside Flathub (~3,000 apps), `@tuxery/matcher`'s pairwise scoring will need a blocking/pre-grouping step (e.g. by normalized-name prefix) to stay tractable — not solved yet, flagged here so it isn't a surprise.
- **Scheduled refresh**: `fetch.ts` implementations are meant to run on a cron schedule, not on every push — see the "Wire scheduled source refresh" card on the Tuxery GitHub Project.
