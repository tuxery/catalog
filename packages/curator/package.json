{
  "name": "@tuxery/curator",
  "version": "0.0.0",
  "private": true,
  "description": "Tuxery's catalog curation: filters out non-app/game packages, then groups the rest across sources by exact app ID or exact normalized name",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint src",
    "test": "vitest run"
  },
  "dependencies": {
    "@tuxery/sources": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "20.19.0",
    "oxlint": "^1.76.0",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
