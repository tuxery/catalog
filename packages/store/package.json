{
  "name": "@tuxery/store",
  "version": "0.0.0",
  "private": true,
  "description": "Tuxery's persistence layer: publishes the built dataset to Cloudflare R2 via its S3-compatible API",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint src",
    "test": "vitest run"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.906.0"
  },
  "devDependencies": {
    "oxlint": "^1.76.0",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
